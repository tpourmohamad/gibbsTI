% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gibbsTI.R
\name{gibbsTI}
\alias{gibbsTI}
\title{Fit a Gibbs Posterior Tolerance Interval}
\usage{
gibbsTI(
  x,
  side = c("one", "two"),
  type = c("upper", "lower"),
  target = c("content", "quantile"),
  content = 0.95,
  confidence = 0.9,
  tau = NULL,
  tau_lower = NULL,
  tau_upper = NULL,
  eta_method = c("GPC", "fixed"),
  eta = NULL,
  max_iter = 15,
  control = list(),
  n_mcmc = 5000,
  burnin = 1000,
  thin = 1,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{x}{Numeric vector of observations (at least 5, all finite).}

\item{side}{Direction of the interval: \code{"one"} (one-sided) or \code{"two"}
(two-sided, centered).}

\item{type}{For one-sided intervals only, \code{"upper"} (bound above) or \code{"lower"}
(bound below). Ignored when \code{side = "two"}.}

\item{target}{Calibration target: \code{"content"} (default; the interval is
defined by a population proportion) or \code{"quantile"} (the interval targets
explicit population quantiles supplied through \code{tau}, \code{tau_lower},
\code{tau_upper}).}

\item{content}{Population proportion the interval should contain, in (0, 1)
(e.g. 0.95). Used when \code{target = "content"}; when \code{target = "quantile"} it
only supplies defaults for any unset \verb{tau_*} argument.}

\item{confidence}{Frequentist confidence level in (0, 1) (e.g. 0.90). This is
both the GPC coverage target (\code{1 - alpha}) and the posterior level read off
as the reported limit.}

\item{tau}{Target quantile for a one-sided \code{"quantile"} interval, in (0, 1).
Only used when \code{target = "quantile"}; ignored when \code{target = "content"}.}

\item{tau_lower, tau_upper}{Lower/upper target quantiles for a two-sided
\code{"quantile"} interval, in (0, 1) with \code{tau_lower < tau_upper}. Only used
when \code{target = "quantile"}; ignored when \code{target = "content"}.}

\item{eta_method}{How the Gibbs learning rate \code{eta} is chosen: \code{"GPC"}
(default; data-driven calibration) or \code{"fixed"} (use the supplied \code{eta}).}

\item{eta}{Gibbs learning rate (positive). Optional starting value when
\code{eta_method = "GPC"} (defaults to 1); required when \code{eta_method = "fixed"}.}

\item{max_iter}{Maximum number of Robbins-Monro iterations for the GPC search
(default 15). Only used when \code{eta_method = "GPC"}.}

\item{control}{A list of control parameters for the MCMC samplers and GPC:
\itemize{
\item \code{B}: Number of bootstrap replicates for calibration (default 200).
\item \code{tol}: Convergence tolerance for eta (default 1e-3).
\item \code{c}, \code{gamma}: Step-size parameters for Robbins-Monro (defaults 0.5, 0.75).
\item \code{w}, \code{m}: Slice sampler width and max steps for one-sided intervals.
\item \code{prop_sd}: Proposal standard deviation for Metropolis-Hastings in the two-sided case.
\item \code{n_samps_calib}: MCMC samples used per bootstrap during calibration (default 1000).
\item \code{burnin_calib}: Burn-in used per bootstrap during calibration (default 200).
}}

\item{n_mcmc}{Number of posterior draws retained for the final interval, after
burn-in and thinning (default 5000).}

\item{burnin}{Number of initial iterations discarded as burn-in (default 1000).}

\item{thin}{Thinning interval: every \code{thin}-th post-burn-in draw is kept
(default 1).}

\item{seed}{Optional integer random seed for reproducibility.}

\item{verbose}{Logical; if \code{TRUE}, prints progress messages.}
}
\value{
An object of class \code{"gibbsTI"}: a list with the fitted
\code{interval} (a numeric limit for one-sided fits, or a list with \code{lower} and
\code{upper} for two-sided fits), the calibrated \code{eta}, the retained posterior
\code{samples}, the \code{data}, the quantile level(s) \code{tau_used}, calibration
diagnostics (\code{calibrated_coverage}, \code{eta_history}), and the \code{settings} used.
Use \code{\link[=print]{print()}}, \code{\link[=summary]{summary()}}, and \code{\link[=plot]{plot()}} on the result.
}
\description{
Constructs a one-sided or two-sided nonparametric tolerance interval from a
calibrated Gibbs posterior. The learning rate \code{eta} is chosen automatically by
Gibbs posterior calibration (GPC) so that the interval attains the nominal
frequentist coverage, or it can be fixed by the user.
}
\examples{
set.seed(1)
x <- rnorm(50)

# Fast: fix the learning rate (no calibration search)
fit <- gibbsTI(x, side = "one", type = "upper", eta_method = "fixed",
               eta = 1, n_mcmc = 500, burnin = 200, verbose = FALSE)
print(fit)

\donttest{
# Data-driven GPC calibration (slower) for a two-sided 95\%/90\% interval
fit2 <- gibbsTI(x, side = "two", content = 0.95, confidence = 0.90,
                seed = 1, verbose = FALSE)
summary(fit2)
}
}
